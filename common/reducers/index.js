import user_info from './user_info';

import home from './home';
import router from './router';

var reducers = {user_info,home,router};

reducers = Object.assign(reducers, {});
module.exports = reducers;